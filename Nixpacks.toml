[phases.setup]
nixPkgs = ['php82', 'php82Extensions.pdo_mysql', 'php82Extensions.mbstring', 'nodejs_20']

[phases.build]
cmds = [
  'composer install --no-dev --optimize-autoloader',
  'npm ci --no-audit',
  'npm run build'
]

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'